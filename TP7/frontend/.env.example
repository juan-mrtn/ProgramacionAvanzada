# URLs del backend
# Copia este archivo a .env.local y ajusta las URLs si es necesario

# URL de la API REST (puerto 3000)
NEXT_PUBLIC_API_URL=http://localhost:3000

# URL del WebSocket Gateway (puerto 3001)
NEXT_PUBLIC_WS_URL=http://localhost:3001
